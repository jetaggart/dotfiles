{
  "name": "lettuce-workspace",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "install-cli": "bun run build && mkdir -p ~/bin/tools && cp dist/index.js ~/bin/tools/lettuce-workspace && chmod +x ~/bin/tools/lettuce-workspace"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.1"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
